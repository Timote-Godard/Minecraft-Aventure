services:
  backend:
    build: ./backend
    network_mode: "host" 
    restart: unless-stopped
    env_file:
      - ./backend/.env 

  frontend:
    build: ./frontend
    network_mode: "host"
    restart: unless-stopped